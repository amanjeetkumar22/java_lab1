package Frame.First;
import javax.swing.*;

public class SwingDemo {
    public static void main(String[] args) {
          /*JFrame frame = new JFrame();
    frame.setTitle("First GUI");
    frame.setSize(420,420);
    frame.setVisible(true);

    // frame.setResizable(false);

    frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    // frame.setDefaultCloseOperation(JFrame.HIDE_ON_CLOSE);
    // frame.setDefaultCloseOperation(JFrame.DO_NOTHING_ON_CLOSE);

    ImageIcon icon = new ImageIcon("images.jpg");
    // ImageIcon icon = new ImageIcon(SwingDemo.class.getResource("images.jpg"));
    frame.setIconImage(icon.getImage());

    // frame.getContentPane().setBackground(Color.BLACK);
    // frame.getContentPane().setBackground(Color.green);
    // frame.getContentPane().setBackground(Color.green);
    // frame.getContentPane().setBackground(new Color(123,50,255));//rgb color code
    // frame.getContentPane().setBackground(Color.gray);
    frame.getContentPane().setBackground(new Color(0x012345));// hexadecimal color coding */

        // MyFrame myframe=new MyFrame();
        new MyFrame();







    }

}
